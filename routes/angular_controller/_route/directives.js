module.exports = function (app) {
  //app.get('/views/directives/:urlname', renderDirective);
}

renderDirective = function renderTest(req, res, next) {
  //res.render('views/directives/' + req.params.urlname);
}
